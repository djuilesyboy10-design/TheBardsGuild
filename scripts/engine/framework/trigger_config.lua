-- trigger_config.lua
-- Configuration data for universal trigger system
-- PART 1 of 3: Data Table
-- DISABLED - Universal trigger system replaced by click_activator
-- All NPC interactions now handled by click-based system for better performance

return {
    triggers = {
        -- ALL TRIGGERS DISABLED
        -- Universal trigger system replaced by click_activator module
        -- This eliminates log spam and improves performance
        -- Use click_activator for all NPC interactions
        
        -- If you need spell-based triggers in the future, 
        -- create a separate spell_activator module
    }
}
