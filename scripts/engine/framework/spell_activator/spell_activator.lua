-- Spell Activator Framework
-- Event-driven spell activation - Global script
-- Part of the Universal Activator Framework

print("[SPELL_ACTIVATOR] Spell activator global script loaded")

return {
    engineHandlers = {
        -- Engine handlers moved to player script
    },
    eventHandlers = {
        -- Global event handlers if needed
    }
}
