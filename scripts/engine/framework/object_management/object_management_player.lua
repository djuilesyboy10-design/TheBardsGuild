local core = require('openmw.core')
local types = require('openmw.types')

return {
    engineHandlers = {
        -- Polling removed: system will transition to event-driven updates
    },
    eventHandlers = {
        -- Placeholder for event-driven logic (e.g., from Quest Monitor)
    }
}
