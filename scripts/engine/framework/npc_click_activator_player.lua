-- NPC Click Activator Player Script
-- Player-side component for NPC click activation
-- Currently empty as global script handles all logic

return {
    -- Player event handlers can be added here if needed
    -- For now, global script handles all NPC click activation
}
